- name: "create ec2 instance and route53"
  hosts: local
  connection: local
  vars:
   ami_id: "ami-09c813fb71547fc4f"
   sg_id: "sg-0269c42f363b9217b"
   hosting_zone: Z02845132921NLJI9M7EG
   domain_name: "devsecops86s.space"
  tasks:
  - name: create ec2 instance
    amazon.aws.ec2_instance:
     name: "{{ item }}"
     instance_type: t3.micro
     security_group: "{{ sg_id }}"
     image_id: "{{ ami_id }}"
     tags:
      name: "{{ item }}"
    loop:
    - mongodb
    - catalogue
    - frontend
    register: ec2_output
  - name: print the ec2 instance output
    ansible.builtin.debug:
      msg: "{{ item.instances[0].private_ip_address}}"
    loop: "{{ ec2_output.results }}"

  - name: route53 record creation
    amazon.aws.route53:
      state: present
      zone: "{{ domain_name }}"
      record: "{{ item.item }}.{{ domain_name }}"
      type: A
      ttl: 1
      value: "{{ item.instances[0].private_ip_address }}"
      overwrite: true
    loop: "{{ ec2_output.results }}"
    register: route53_output
  - name: print the record output
    ansible.builtin.debug:
      msg: "{{ item.name }}"
    loop: "{{ route53_output.results }}"

  - name: route53 record creation for frontend
    amazon.aws.route53:
      state: present
      zone: "{{ domain_name }}"
      record: "{{ domain_name }}"
      type: A
      ttl: 1
      value: "{{ item.instances[0].public_ip_address }}"
      overwrite: true
    when: item.item == "frontend"
    loop: "{{ ec2_output.results }}"
    register: route53_output
  - name: print the frontendrecord output
    ansible.builtin.debug:
        msg: "{{ item.name }}"
    loop: "{{ route53_output.results }}"



